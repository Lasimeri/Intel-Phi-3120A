# toolchain/llvm/patches

Empty until phase P2. Patches are named `NNNN-<summary>.patch`, generated
with `git format-patch` against the tag recorded in `SERIES`, and each
carries the LLVM Apache-2.0-with-LLVM-exception license of the code it
modifies.
