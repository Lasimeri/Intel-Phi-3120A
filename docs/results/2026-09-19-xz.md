# 2026-09-19: xz 5.8.3 on the card, and what LZMA costs here

Question asked: what compression throughput can the card reach, and how
does it compare with the host running the same code.

Host 7.2.6-1-cachyos, Ryzen 7 5800X, 16 threads, xz 5.8.3 from Arch. Card
running xz 5.8.3 built by `card/userland/components/xz.sh` with the same
source, same version, 0 illegal instructions.

Corpus: 473 MiB `tar` of the Linux 7.2.3 tree (`arch drivers/net
drivers/gpu/drm/i915 kernel lib mm fs include sound`), built with
`--sort=name --owner=0 --group=0 --numeric-owner --mtime=2026-01-01` so it
is byte-identical on both machines, and transferred to the card with
`phi put` at 10.8 MB/s.

## Results

| Machine | Preset | Threads | Blocks | Time | Throughput | Ratio |
| --- | --- | --- | --- | --- | --- | --- |
| card | -1 | 16 | 157 | 21 s | 22 MB/s | |
| card | -1 | 57 | 157 | 7 s | 67 MB/s | |
| card | -1 | 114 | 157 | 7 s | 67 MB/s | |
| card | -1 | 228 | 157 | 6 s | **78 MB/s** | |
| host | -1 | 1 | 157 | 11.30 s | 41.8 MB/s | |
| host | -1 | 8 | 157 | 1.87 s | 252.3 MB/s | |
| host | -1 | 16 | 157 | 1.60 s | **295.9 MB/s** | |
| card | -6 | 8 | 19 | 319 s | 1.5 MB/s | 13% |
| card | -6 | 19 | 19 | 151 s | 3.1 MB/s | 13% |
| card | -6 | 114 | 118 (4 MiB) | 44 s | **10.7 MB/s** | 13% |
| card | -6 | 228 | 236 (2 MiB) | 111 s | 4.3 MB/s | 14% |
| host | -6 | 8 | 19 | 17.33 s | 27.2 MB/s | 13% |
| host | -6 | 16 | 19 | 14.08 s | 33.5 MB/s | 13% |
| host | -6 | 16 | 118 (4 MiB) | 9.91 s | **47.6 MB/s** | 13% |

## Readings

**The card is about a quarter of the host, at best.** 78 against 296 MB/s
at `-1` (0.26x), 10.7 against 47.6 at `-6` (0.22x). That is better than the
card's pure integer ratio of 0.14x (`2026-09-15-pi-bbp.md`), and the reason
is that LZMA's match finder is a pointer chase through a binary tree:
228 threads hide memory latency that stalls a 16-thread host. It is far
worse than the card's 2.25x memory-bandwidth advantage
(`card/examples/membw.md`), because the match finder is compute-heavy, not
stream-heavy.

**Tuning is worth 7x, and the defaults are terrible.** At `-6` the card
goes from 1.5 MB/s to 10.7 MB/s purely by changing thread count and block
size. Nothing about the data or the algorithm changed. The default block
size at `-6` is 3x the 8 MiB dictionary, so a 473 MiB input makes only 19
blocks and *at most 19 of 228 threads can ever run*. Block count, not
thread count, is the real parallelism limit in xz, and on a 228-thread
machine that bites immediately.

**Four threads per core is worse than two, again.** `-T228` with 2 MiB
blocks (4.3 MB/s) lost to `-T114` with 4 MiB blocks (10.7 MB/s). Two
effects compound: smaller blocks cost per-block encoder setup and a
slightly worse ratio (14% against 13%), and 228 threads put four on every
in-order core where they contend for one L2 port. The independent
bandwidth measurement found the same turnover at two threads per core
(80.5 GB/s at 114, 66.0 at 228), so this is the same wall showing up in a
real workload rather than a microbenchmark artifact.

**Memory was not the limit, contention was.** The `-T114` run touched only
37 MiB of swap. At `-6` liblzma wants roughly 94 MiB per encoder thread, so
114 threads would need about 10 GiB and should have thrashed; it did not,
because with 4 MiB blocks the per-thread footprint falls with the block
size. The card's 6 GB does cap high presets: at `-9` (64 MiB dictionary,
roughly 700 MiB per thread) only about 7 threads fit, and the card has no
answer there at all.

**Compression ratio is identical.** 13% on both machines at both presets,
so this is purely a speed comparison; the card produces the same output.

## Verdict

Not worth offloading. The host does the same job four times faster and
needs no tuning to do it. What the measurement is actually good for is
calibration: xz is a realistic, pointer-chasing, memory-latency-bound
integer workload, and the card lands at 0.22 to 0.26x on it. That sits
exactly between its 0.14x on pure integer work and its 2.25x on pure
bandwidth, which is a useful anchor for judging any future candidate
workload before porting it.

## A portability bug found on the way

`src/liblzma/rangecoder/range_decoder.h` enables hand-written x86-64 inline
assembly whenever `__x86_64__` is defined:

```c
#ifndef LZMA_RANGE_DECODER_CONFIG
#	if defined(__x86_64__) && !defined(__ILP32__) ...
#		define LZMA_RANGE_DECODER_CONFIG 0x1F0
```

It uses `__x86_64__` as a proxy for "has CMOV". The card is x86-64 without
CMOV, so the assembly compiles in and no compiler flag can remove it:
`phi-isa-audit` found 216 `cmov` instructions, all in `lzma_decoder.o`.
`-DLZMA_RANGE_DECODER_CONFIG=0` selects the portable C path that every
non-x86 target already uses. This is the class of assumption the card is
good at exposing, and it is the same reason `docs/plan.md` lists the card
as a differential-testing partner.

Two build traps are recorded in `xz.md`: `--disable-shared` governs only
liblzma and leaves the driver dynamic (the card has no loader, so it fails
with a bare "not found"), and libtool filters `-static` out of the program
link, so the script verifies the result and relinks by hand.
